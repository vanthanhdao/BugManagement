

@using QuanlyBug.Models
@{
    USER kh = (USER)Session["TaiKhoan"];


}




@if (Session["TaiKhoan"] == null)

{
    @Url.Action("Index","About")
}
else
{

    <div class="nav-item dropdown">
        <a href="#" class="nav-link d-flex lh-1 text-reset p-0" data-bs-toggle="dropdown" aria-label="Open user menu">
            <span class="avatar avatar-sm" ><img src="~/Assets/static/avatars/000m.jpg" /></span>
            <div class="d-none d-xl-block ps-2">
                <div>@kh.Username</div>
                <div class="mt-1 small text-muted">@kh.Email</div>
            </div>
        </a>
        <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
            <a href="./profile.html" class="dropdown-item">Trang cá nhân</a>
            <a href="@Url.Action("LogOut","User")" class="dropdown-item">Đăng xuất</a>
          

        </div>
    </div>
}
